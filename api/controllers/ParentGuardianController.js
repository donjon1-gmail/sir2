/**
 * ParentGuardianController
 *
 * @description :: Server-side logic for managing Parentguardians
 * @help        :: See http://sailsjs.org/#!/documentation/concepts/Controllers
 */

module.exports = {


/*afterCreate: function(values, cb) {
    if (values.address) {
      return address.update({id: values.address}, {ParentGuardian: values.id}).exec(cb);
    }
    return cb();
  },
*/	
};

