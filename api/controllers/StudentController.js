/**
 * StudentController
 *
 * @description :: Server-side logic for managing Students
 * @help        :: See http://sailsjs.org/#!/documentation/concepts/Controllers
 */
module.exports = {

/*
afterCreate: function(values, cb) {
    if (values.address) {
      return address.update({id: values.address}, {Student: values.id}).exec(cb);
    }
    return cb();
  },
*/
/*	find: function(values,cb){
		populate("grades").exec(
			function(err,result){});
	}*/
	
};

